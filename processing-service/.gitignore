# Service-specific binaries
processing-service
processing-service.exe

# Processing temp files
/processing-temp/
/ffmpeg-temp/

# Local config files
config-local.yaml
.env.local

# Security: Sensitive files
*credentials*
*_credentials*
*-credentials*
*.pem
*.key
*.crt
keyPrincipal*
*keyPrincipal*
*-token*
*-secret*
*secret*
*password*

# AWS and SES credentials
*aws-credentials*
*ses-credentials*
*smtp-credentials*

# Context files (not for version control)
.service-context.md
