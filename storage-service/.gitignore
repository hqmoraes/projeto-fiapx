# Service-specific binaries
storage-service
storage-service.exe

# Local storage cache
/local-cache/

# Local config files
config-local.yaml
.env.local

# Security: Sensitive files
*credentials*
*_credentials*
*-credentials*
*.pem
*.key
*.crt
keyPrincipal*
*keyPrincipal*
*-token*
*-secret*
*secret*
*password*

# AWS and SES credentials
*aws-credentials*
*ses-credentials*
*smtp-credentials*
