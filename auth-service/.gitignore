# Service-specific binaries
auth-service
auth-service.exe

# Local config files
config-local.yaml
.env.local

# Security: Sensitive files
*credentials*
*_credentials*
*-credentials*
*.pem
*.key
*.crt
keyPrincipal*
*keyPrincipal*
*-token*
*-secret*
*secret*
*password*

# AWS and SES credentials
*aws-credentials*
*ses-credentials*
*smtp-credentials*
